import 'package:flutter/material.dart';

class StockCharts extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        'Stock Charts',
        style: TextStyle(color: Colors.white, fontSize: 20),
      ),
    );
  }
}
